函数通过将返回值复制到指定的CPU寄存器或内存单元来将其返回。
随后调用程序查看该内存但元，返回函数和调用函数必须就该但元
内存中存储的数据的类型达成一致。函数原型将返回值类型告知
调用程序，而函数定义命令被调用函数应返回什么类型


//you cannot change age via pt
int age = 39;
const int *pt = &age;


//stck can only point to trouble
const double *const stck = &trouble

